# BUG-001: Explicit falsy configuration values are ignored

## Observed

- `max_retries=0` becomes `3`.
- `feature_enabled=False` becomes `True`.

## Expected

- A missing value or `None` uses the default.
- Explicit `0` remains `0`.
- Explicit `False` remains `False`.
- No unrelated behavior changes.

