# Agentic Bugfix Demo

This repository contains an intentionally buggy Python target application and a trusted,
API-driven multi-agent workflow that investigates, reproduces, fixes, and reviews the defect.

The complete architecture, security boundaries, commands, and recording guide are added with
the controller implementation.

